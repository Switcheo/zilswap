test('add stall to emporium', async () => {})
test('add stall as resource minter', async () => {})
test('add item (resource) to resource stall -> emporium', async () => {})
test('purchase item (resource) from emporium -> resource stall', async () => {
  // test inflation mech
  // first item = current inflation bps
  // second item onwards = current inflation bps + (n-1) * increment 
  // limit by max price
})